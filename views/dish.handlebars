<ul>
  {{#forEach players as ||player}}
  
</ul>




<div class="artist-card">
  <div class="artist-card-header">
    {{artist}}
  </div>
  <div class="card-body">
    <p>
      Description:
      {{player.artist}}
    </p>
    <p>
      Guest:
      {{player.track}}
    </p>
    {{#if has_nuts}}
      <p class="has-nuts">
        Contains Nuts
      </p>
    {{/if}}
  </div>
</div>